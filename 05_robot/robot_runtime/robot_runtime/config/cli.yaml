# ============================================================================
# CLI 工具配置
# ============================================================================
cli:
  default_config_dir: config
  default_log_dir: log
  default_workspace: .
